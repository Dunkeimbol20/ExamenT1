<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Estado presionado -->
    <item android:state_pressed="true" android:drawable="@color/purple_200"/>
    <!-- Estado seleccionado -->
    <item android:state_selected="true" android:drawable="@color/primary_principal"/>
    <!-- Estado normal -->
    <item android:drawable="@color/blanco"/>
</selector>